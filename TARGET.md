# Target

Task:

- Improve `spot` E2E model validation performance using temporal-head-only
  research.

Metric:

- `best_val_mAP` from training artifacts (`loss.json`).

Data:

- `/data/lyx/spot/frames/soccernetv2`

Model focus:

- temporal head selection and lightweight temporal head variants.
