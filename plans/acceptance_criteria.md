# Acceptance Criteria

Minimal workflow is considered ready when all conditions are met:

1. A manifest can generate valid training commands.
2. A finished run directory can be parsed to extract `best_val_mAP`.
3. Leaderboard can be generated/updated from parsed results.
4. `program.md` constraints clearly limit edits to temporal heads.
